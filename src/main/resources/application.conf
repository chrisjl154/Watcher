example = "TestExample"

http-config {
  host = "http://localhost"
  port = "5000"
  max-concurrent-requests = 10
}

application-metric-processing-config {
  stream-sleep-time = 20
  stream-parallelism-max = 20
}